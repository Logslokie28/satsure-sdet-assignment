# Task 6: AI Reflection

- **Tools Used:** Gemini / ChatGPT / Claude.
- **Usage Areas:** Test scenario brainstorming, Playwright Page Object Model structural generation, and regex creation for JSON schema assertions.
- **Modifications Made:**
  1. *Correction 1:* Fixed space-separated JSON keys (`start date`) generated by the AI to match the required snake_case format (`start_date`).
  2. *Correction 2:* Updated the returned string `"true"` in AI assertions to strictly assert an explicit boolean `true`.
- **AI Limitations:** AI failed to spot that the sample JSON payload contained invalid syntax due to a trailing comma and returned UTC (`Z`) time instead of the required local IST offset.
